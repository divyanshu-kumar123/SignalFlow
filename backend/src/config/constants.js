// Centralized file for application-wide static data
// This all should be in db but for simplicity as it is just a demo storing the data here.
export const SUPPORTED_ASSETS = ['BTC', 'ETH', 'NIFTY50', 'SOL', 'RELIANCE'];

export const DEFAULT_PRICES = {
  BTC: 65000,
  ETH: 3500,
  NIFTY50: 22000,
  SOL:1000,
  RELIANCE:900
};